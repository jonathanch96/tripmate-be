x-backend-environment: &backend-environment
  APP_ENV: ${APP_ENV:-production}
  APP_PORT: 8080
  DB_HOST: ${DB_HOST:-postgres-db}
  DB_PORT: ${DB_PORT:-5432}
  DB_USER: ${DB_USER:-tripmate}
  DB_PASSWORD: ${DB_PASSWORD:?set DB_PASSWORD in tripmate-be/.env}
  DB_NAME: ${DB_NAME:-tripmate}
  DB_SCHEMA: ${DB_SCHEMA:-tripmate}
  DB_SSLMODE: ${DB_SSLMODE:-disable}
  DB_AUTO_MIGRATE: "false"
  JWT_ACCESS_SECRET: ${JWT_ACCESS_SECRET:?set JWT_ACCESS_SECRET in tripmate-be/.env}
  JWT_REFRESH_SECRET: ${JWT_REFRESH_SECRET:?set JWT_REFRESH_SECRET in tripmate-be/.env}
  GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID:-}
  GEMINI_API_KEY: ${GEMINI_API_KEY:-}
  GEMINI_MODEL: ${GEMINI_MODEL:-gemini-1.5-flash}
  CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS:-https://tripmate.jonathanchang.my.id}
  STORAGE_LOCAL_ROOT: /data/receipts
  STORAGE_PUBLIC_URL: ${STORAGE_PUBLIC_URL:-http://localhost:8080/api/v1/receipt-images}

services:
  migrate:
    image: ${BACKEND_IMAGE:-tripmate-backend:local}
    build:
      context: .
      dockerfile: Dockerfile.prod
    entrypoint: ["/tripmate-migrate"]
    environment: *backend-environment
    networks:
      - shared_network
    restart: "no"
    security_opt:
      - no-new-privileges:true

  backend:
    image: ${BACKEND_IMAGE:-tripmate-backend:local}
    build:
      context: .
      dockerfile: Dockerfile.prod
    environment: *backend-environment
    depends_on:
      migrate:
        condition: service_completed_successfully
    ports:
      - "${BACKEND_BIND_ADDRESS:-127.0.0.1}:${BACKEND_PORT:-8080}:8080"
    volumes:
      - receipts-data:/data/receipts
    networks:
      shared_network:
        aliases:
          - tripmate-backend
    init: true
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp:size=32m,mode=1777
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true

networks:
  shared_network:
    external: true
    name: shared_network

volumes:
  receipts-data:
